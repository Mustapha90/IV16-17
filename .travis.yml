language: python
python:
  - "2.7"

# instalar las dependencias
install: 
 - make install_dev

# crear la base de datos
before_script: 
 - make migrate
 - make populate

# ejecutar tests
script: 
 - make test
